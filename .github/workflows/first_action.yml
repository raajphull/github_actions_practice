name: first_workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: checkout action
      uses: actions/checkout@v4


    - name: welcome_message
      run: echo "I h8 pie"

    - name: list_files
      run: ls -la
    
    - name: readme_file
      run: cat ./README.md
